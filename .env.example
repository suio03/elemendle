# Server-only Discord webhook for the dedicated Elemendle feedback channel.
# Copy this setting into .env.local for local development.
# Configure the same name as a Cloudflare Worker secret before deployment.
DISCORD_FEEDBACK_WEBHOOK_URL=
